enable_cli: true

api_auth:
  - name: "llm_testing_token"
    token: "for_the_llm"
    scopes:
      - list
      - logs
      - get
      - health
      - start
    allowed_components:
      - sni5gect
      - sstorm
      - rtue
    pass_to:
      - sniffer_test_worker

build_spec:
  - component: "cueltschey/rt-env-test"
    branch: "main"
    pull: false


run_spec:
  - component: "cueltschey/rt-env-test"
    name: "test"
    rf:
      type: "none"
